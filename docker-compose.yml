version: '3.8'

services:
  # Web server (PHP + Apache)
  web:
    build: .
    container_name: shopaccgame_web
    ports:
      - "80:80"
    volumes:
      - ./src/cuoi_ki_web:/var/www/html
    environment:
      # QUAN TRỌNG: Thay đổi địa chỉ DB thành Private IP của máy EC2-2
      - DB_HOST=database-mysql.ch2wsgi4quts.ap-southeast-1.rds.amazonaws.com # <--- Điền Private IP của máy DB vào đây
      - DB_USER=phuoc
      - DB_PASSWORD=phuoc2209
      - DB_NAME=shopaccgame
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
